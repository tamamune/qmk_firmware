#pragma once

// 設定したコンボ（同時押し）の数
#define COMBO_COUNT 2

// コンボのタイミング 30ミリ秒以内に同時押ししないと発動しない
#define COMBO_TERM 30 

// マウスを止めてから通常レイヤーに戻るまでの時間（ミリ秒単位）
// 500なら「0.5秒間」ボールが動かないと元のレイヤーに戻ります。
#define AUTO_MOUSE_TIME 500
